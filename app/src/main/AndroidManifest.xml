<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mitmit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MitMit">
        <activity android:name=".Activite_Loisirs_Description"></activity>
        <activity android:name=".test" />
        <activity android:name=".LoisirDescriptionActivity" />
        <activity android:name=".DescriptionLoisirActivity" />
        <activity android:name=".recyclerview.LoisirActivite" />
        <activity android:name=".Authentification" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>